services:
  kafka:
    image: confluentinc/cp-kafka:7.6.1
    container_name: kafka
    hostname: kafka
    ports:
      - "29092:29092"
    environment:
      CLUSTER_ID: "MkU3OEVBNTcwNTJENDM2Qk"
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: "broker,controller"
      KAFKA_LISTENERS: "PLAINTEXT://0.0.0.0:9092,PLAINTEXT_HOST://0.0.0.0:29092,CONTROLLER://0.0.0.0:9093"
      KAFKA_ADVERTISED_LISTENERS: "PLAINTEXT://kafka:9092,PLAINTEXT_HOST://localhost:29092"
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: "PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT,CONTROLLER:PLAINTEXT"
      KAFKA_CONTROLLER_LISTENER_NAMES: "CONTROLLER"
      KAFKA_CONTROLLER_QUORUM_VOTERS: "1@kafka:9093"
      KAFKA_INTER_BROKER_LISTENER_NAME: "PLAINTEXT"
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
    volumes:
      - kafka_data:/var/lib/kafka/data

  mlflow:
    image: python:3.10-slim
    container_name: mlflow
    working_dir: /app
    command: >
      bash -c "pip install mlflow && mlflow server
      --backend-store-uri sqlite:///mlflow.db
      --default-artifact-root /app/mlruns
      --host 0.0.0.0
      --port 5000"
    ports:
      - "5001:5000"
    volumes:
      - ./mlflow_data:/app

  api:
    build:
      context: ..
      dockerfile: docker/Dockerfile.api
    container_name: crypto-api
    depends_on:
      - kafka
      - mlflow
    environment:
      MODEL_PATH: "/app/models/artifacts/random_forest_pipeline.pkl"
      APP_VERSION: "v1.2"
      MODEL_NAME: "random_forest"
      MODEL_VARIANT: "${MODEL_VARIANT:-ml}"
      GIT_SHA: "${GIT_SHA:-handoff}"
      BASELINE_CONFIG_PATH: "/app/models/artifacts/baseline_zscore_config.json"
      KAFKA_BOOTSTRAP_SERVERS: "kafka:9092"
      KAFKA_LAG_ENABLED: "true"
      KAFKA_LAG_GROUP_ID: "crypto-featurizer"
      KAFKA_LAG_TOPIC: "ticks.raw"
    ports:
      - "8000:8000"
    volumes:
      - ..:/app

  ingestor:
    build:
      context: ..
      dockerfile: handoff/Dockerfile.ingestor
    container_name: ingestor
    working_dir: /app
    depends_on:
      - kafka
    command: ["python", "scripts/ws_ingest.py", "--pair", "BTC-USD", "--minutes", "15", "--bootstrap_servers", "kafka:9092"]

volumes:
  kafka_data: